name: ci

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  ci:
    uses: mpa-forge/.github/.github/workflows/reusable-ci-contracts.yml@816e99050519226be282df48aaa86cc9d40227f9
    with:
      working-directory: "."
      go-version-file: "go.mod"
      node-version: "24.13.1"
      buf-version: "1.65.0"
